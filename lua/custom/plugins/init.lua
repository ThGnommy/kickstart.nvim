-- Custom plugins are loaded from this directory automatically.
-- Add new plugin specs as individual files (e.g. myplugin.lua) and they
-- will be picked up by `{ import = 'custom.plugins' }` in init.lua.
return {}
